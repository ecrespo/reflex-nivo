"""Reflex wrappers for @nivo/circle-packing.

Generated by scripts/generate_components.py -- do not edit by hand.
"""

from __future__ import annotations

from collections.abc import Sequence
from typing import Any, ClassVar, Literal

from reflex.event import EventHandler
from reflex.vars.base import Var

from ..base import NivoComponent, nivo_event_spec, nivo_id_event_spec
from ..constants import nivo_package


class CirclePacking(NivoComponent):
    """Circle packing of hierarchical data.

    Wraps ``ResponsiveCirclePacking`` from ``@nivo/circle-packing``.
    Docs: https://nivo.rocks/circle-packing/
    """

    library = nivo_package("circle-packing")
    tag = "ResponsiveCirclePacking"
    _nivo_variant: ClassVar[str] = "svg"

    # Enable/disable transitions.
    animate: Var[bool]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    border_color: Var[Any]
    # TS: number | undefined
    border_width: Var[int | float]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    child_color: Var[Any]
    # TS: CircleComponent<Datum> | undefined
    circle_component: Var[Any]
    # TS: "id" | "depth" | undefined
    color_by: Var[Literal["id", "depth"]]
    # Colors: {'scheme': 'nivo'}, a list of colors, a color, or a JS function.
    colors: Var[Any]
    # Chart data (see the nivo docs of this chart for the expected shape). (required)
    data: Var[Any]
    # Debounce (ms) applied to container resize detection.
    debounce_resize: Var[int | float]
    # Height used before the container has been measured.
    default_height: Var[int | float]
    # Width used before the container has been measured.
    default_width: Var[int | float]
    # SVG pattern/gradient definitions (see reflex_nivo.pattern_dots_def & co).
    defs: Var[Any]
    # TS: boolean | undefined
    enable_labels: Var[bool]
    # Rules matching data to defs: [{'match': {'id': 'x'}, 'id': 'dots'}].
    fill: Var[Any]
    # Accessor for the datum id (nivo's `id` prop): a key name or JS function.
    id_by: Var[Any]
    # TS: boolean | undefined
    inherit_color_from_parent: Var[bool]
    # Enable tooltips and mouse events.
    is_interactive: Var[bool]
    # TS: PropertyAccessor<ComputedDatum<Datum>, string> | undefined
    label: Var[Any]
    # TS: LabelComponent<Datum> | undefined
    label_component: Var[Any]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    label_text_color: Var[Any]
    # TS: ((label: ComputedLabel<Datum>) => boolean) | undefined
    labels_filter: Var[Any]
    # TS: number | undefined
    labels_skip_radius: Var[int | float]
    # Layers to render, in order (strings or custom JS layers).
    layers: Var[Any]
    # TS: boolean | undefined
    leaves_only: Var[bool]
    # Chart margin: {'top': .., 'right': .., 'bottom': .., 'left': ..}.
    margin: Var[Any]
    # react-spring preset ('gentle', 'wobbly', ...) or a config dict.
    motion_config: Var[Any]
    # TS: number | undefined
    padding: Var[int | float]
    # Render the wrapping div nivo uses for tooltips.
    render_wrapper: Var[bool]
    # ARIA role of the chart root.
    role: Var[str]
    # nivo theme; defaults to reflex_nivo.themes.auto() (follows color mode).
    theme: Var[Any]
    # Tooltip component: use reflex_nivo.tooltip('{template}') or reflex_nivo.js(...).
    tooltip: Var[Any]
    # TS: PropertyAccessor<Datum, number> | undefined
    value: Var[Any]
    # d3-format specifier (e.g. ' >-.2f') or JS function.
    value_format: Var[Any]
    # TS: string | null | undefined
    zoomed_id: Var[str | None]

    # Events: the handler receives the JSON-safe first callback argument.
    on_click: EventHandler[nivo_event_spec]
    on_mouse_enter: EventHandler[nivo_event_spec]
    on_mouse_leave: EventHandler[nivo_event_spec]
    on_mouse_move: EventHandler[nivo_event_spec]
    on_resize: EventHandler[nivo_event_spec]


class CirclePackingCanvas(NivoComponent):
    """Circle packing rendered on canvas.

    Wraps ``ResponsiveCirclePackingCanvas`` from ``@nivo/circle-packing``.
    Docs: https://nivo.rocks/circle-packing/canvas/
    """

    library = nivo_package("circle-packing")
    tag = "ResponsiveCirclePackingCanvas"
    _nivo_variant: ClassVar[str] = "canvas"

    # Enable/disable transitions.
    animate: Var[bool]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    border_color: Var[Any]
    # TS: number | undefined
    border_width: Var[int | float]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    child_color: Var[Any]
    # TS: "id" | "depth" | undefined
    color_by: Var[Literal["id", "depth"]]
    # Colors: {'scheme': 'nivo'}, a list of colors, a color, or a JS function.
    colors: Var[Any]
    # Chart data (see the nivo docs of this chart for the expected shape). (required)
    data: Var[Any]
    # Debounce (ms) applied to container resize detection.
    debounce_resize: Var[int | float]
    # Height used before the container has been measured.
    default_height: Var[int | float]
    # Width used before the container has been measured.
    default_width: Var[int | float]
    # TS: boolean | undefined
    enable_labels: Var[bool]
    # Accessor for the datum id (nivo's `id` prop): a key name or JS function.
    id_by: Var[Any]
    # TS: boolean | undefined
    inherit_color_from_parent: Var[bool]
    # Enable tooltips and mouse events.
    is_interactive: Var[bool]
    # TS: PropertyAccessor<ComputedDatum<Datum>, string> | undefined
    label: Var[Any]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    label_text_color: Var[Any]
    # TS: ((label: ComputedLabel<Datum>) => boolean) | undefined
    labels_filter: Var[Any]
    # TS: number | undefined
    labels_skip_radius: Var[int | float]
    # Layers to render, in order (strings or custom JS layers).
    layers: Var[Any]
    # TS: boolean | undefined
    leaves_only: Var[bool]
    # Chart margin: {'top': .., 'right': .., 'bottom': .., 'left': ..}.
    margin: Var[Any]
    # react-spring preset ('gentle', 'wobbly', ...) or a config dict.
    motion_config: Var[Any]
    # TS: number | undefined
    padding: Var[int | float]
    # Canvas pixel ratio (defaults to window.devicePixelRatio).
    pixel_ratio: Var[int | float]
    # Render the wrapping div nivo uses for tooltips.
    render_wrapper: Var[bool]
    # ARIA role of the chart root.
    role: Var[str]
    # nivo theme; defaults to reflex_nivo.themes.auto() (follows color mode).
    theme: Var[Any]
    # Tooltip component: use reflex_nivo.tooltip('{template}') or reflex_nivo.js(...).
    tooltip: Var[Any]
    # TS: PropertyAccessor<Datum, number> | undefined
    value: Var[Any]
    # d3-format specifier (e.g. ' >-.2f') or JS function.
    value_format: Var[Any]
    # TS: string | null | undefined
    zoomed_id: Var[str | None]

    # Events: the handler receives the JSON-safe first callback argument.
    on_click: EventHandler[nivo_event_spec]
    on_mouse_move: EventHandler[nivo_event_spec]
    on_resize: EventHandler[nivo_event_spec]


class CirclePackingHtml(NivoComponent):
    """Circle packing rendered with HTML elements.

    Wraps ``ResponsiveCirclePackingHtml`` from ``@nivo/circle-packing``.
    Docs: https://nivo.rocks/circle-packing/html/
    """

    library = nivo_package("circle-packing")
    tag = "ResponsiveCirclePackingHtml"
    _nivo_variant: ClassVar[str] = "html"

    # Enable/disable transitions.
    animate: Var[bool]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    border_color: Var[Any]
    # TS: number | undefined
    border_width: Var[int | float]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    child_color: Var[Any]
    # TS: CircleComponent<Datum> | undefined
    circle_component: Var[Any]
    # TS: "id" | "depth" | undefined
    color_by: Var[Literal["id", "depth"]]
    # Colors: {'scheme': 'nivo'}, a list of colors, a color, or a JS function.
    colors: Var[Any]
    # Chart data (see the nivo docs of this chart for the expected shape). (required)
    data: Var[Any]
    # Debounce (ms) applied to container resize detection.
    debounce_resize: Var[int | float]
    # Height used before the container has been measured.
    default_height: Var[int | float]
    # Width used before the container has been measured.
    default_width: Var[int | float]
    # TS: boolean | undefined
    enable_labels: Var[bool]
    # Accessor for the datum id (nivo's `id` prop): a key name or JS function.
    id_by: Var[Any]
    # TS: boolean | undefined
    inherit_color_from_parent: Var[bool]
    # Enable tooltips and mouse events.
    is_interactive: Var[bool]
    # TS: PropertyAccessor<ComputedDatum<Datum>, string> | undefined
    label: Var[Any]
    # TS: LabelComponent<Datum> | undefined
    label_component: Var[Any]
    # TS: InheritedColorConfig<ComputedDatum<Datum>> | undefined
    label_text_color: Var[Any]
    # TS: ((label: ComputedLabel<Datum>) => boolean) | undefined
    labels_filter: Var[Any]
    # TS: number | undefined
    labels_skip_radius: Var[int | float]
    # Layers to render, in order (strings or custom JS layers).
    layers: Var[Any]
    # TS: boolean | undefined
    leaves_only: Var[bool]
    # Chart margin: {'top': .., 'right': .., 'bottom': .., 'left': ..}.
    margin: Var[Any]
    # react-spring preset ('gentle', 'wobbly', ...) or a config dict.
    motion_config: Var[Any]
    # TS: number | undefined
    padding: Var[int | float]
    # Render the wrapping div nivo uses for tooltips.
    render_wrapper: Var[bool]
    # ARIA role of the chart root.
    role: Var[str]
    # nivo theme; defaults to reflex_nivo.themes.auto() (follows color mode).
    theme: Var[Any]
    # Tooltip component: use reflex_nivo.tooltip('{template}') or reflex_nivo.js(...).
    tooltip: Var[Any]
    # TS: PropertyAccessor<Datum, number> | undefined
    value: Var[Any]
    # d3-format specifier (e.g. ' >-.2f') or JS function.
    value_format: Var[Any]
    # TS: string | null | undefined
    zoomed_id: Var[str | None]

    # Events: the handler receives the JSON-safe first callback argument.
    on_click: EventHandler[nivo_event_spec]
    on_mouse_enter: EventHandler[nivo_event_spec]
    on_mouse_leave: EventHandler[nivo_event_spec]
    on_mouse_move: EventHandler[nivo_event_spec]
    on_resize: EventHandler[nivo_event_spec]


circle_packing = CirclePacking.create
circle_packing_canvas = CirclePackingCanvas.create
circle_packing_html = CirclePackingHtml.create

__all__ = [
    "CirclePacking",
    "CirclePackingCanvas",
    "CirclePackingHtml",
    "circle_packing",
    "circle_packing_canvas",
    "circle_packing_html",
]
